class Solution {
public:
    bool isPowerOfFour(int num) {
        //数学问题
        return num > 0 && (num & (num - 1)) == 0 && (num - 1) % 3 == 0;
        //二项式定理
        //(1+x)^n,可以得出当x=3时，4^-1时3的倍数
    }
};
